// ============= Import Modules =============

// ============= Import Components =============
import Hero from "../components/hero/Hero";

const Homepage = () => {
  return (
    <div>
      <Hero />
    </div>
  );
};

export default Homepage;
